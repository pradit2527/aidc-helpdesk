/**
 * เปลือกของหน้าที่ยังไม่ผ่านการยืนยันตัวตน
 *
 * ไม่ห่อเนื้อหาด้วยการ์ดกลางจอ เพราะหน้าเข้าสู่ระบบตามต้นแบบเป็นสองคอลัมน์
 * เต็มจอ ส่วนหน้าเปลี่ยนรหัสผ่านจัดกึ่งกลางเองในหน้าของมัน
 *
 * ไม่มีเมนู ไม่มีลิงก์ไปหน้าอื่นโดยตั้งใจ — ยังไม่รู้ว่าเป็นใคร
 * จึงยังไม่รู้ว่าควรเห็นเมนูชุดไหน
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <main id="main" className="min-h-screen bg-page">
      {children}
    </main>
  );
}
