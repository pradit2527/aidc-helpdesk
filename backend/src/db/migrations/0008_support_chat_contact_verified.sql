-- ============================================================================
-- 0008_support_chat_contact_verified
--
-- เตรียมรับระบบอื่นที่มีล็อกอินของตัวเองมาต่อกับ Support Hub
--
-- ธงนี้บอกว่า Chatwoot ยืนยันตัวตนผู้เข้าชมด้วย HMAC แล้ว (meta.hmac_verified
-- ของบทสนทนา) ซึ่งเป็นเงื่อนไข **เดียว** ที่ยอมให้จับคู่อีเมลของผู้เข้าชม
-- กับบัญชีใน Helpdesk — อีเมลจากฟอร์มก่อนแชทที่ไม่ผ่าน HMAC ใครก็พิมพ์ของคนอื่นได้
--
-- แถวเดิมได้ false ทั้งหมด ซึ่งถูกต้อง: ไม่มีแถวไหนเคยถูกยืนยันมาก่อน
-- ============================================================================

ALTER TABLE "support_chat" ADD COLUMN "contact_verified" boolean DEFAULT false NOT NULL;

-- ============================================================================
-- down
-- ============================================================================
-- ALTER TABLE support_chat DROP COLUMN contact_verified;
