# AIDC Helpdesk — Backend (ADR-002)
# Python 3.12  ·  pin เวอร์ชันย่อยเพื่อให้ build ซ้ำได้เหมือนเดิมเสมอ

# ── Web framework ──
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20

# ── ฐานข้อมูล ──
sqlalchemy[asyncio]==2.0.37
alembic==1.14.1
psycopg[binary,pool]==3.2.4

# ── Validation / settings ──
pydantic==2.10.6
pydantic-settings==2.7.1
email-validator==2.2.0

# ── ความปลอดภัย ──
pyjwt==2.10.1
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# ── งานเบื้องหลัง ──
celery==5.4.0
redis==5.2.1

# ── Export ──
openpyxl==3.1.5
weasyprint==63.1          # ต้องมี libthai0 + pango ใน image (ดู Dockerfile)
jinja2==3.1.5

# ── ไฟล์แนบ ──
python-magic==0.4.27      # ตรวจ MIME จาก magic bytes จริง ไม่เชื่อ Content-Type
python-magic-bin==0.4.14; sys_platform == "win32"

# ── Log ──
structlog==25.1.0

# ── เขตเวลา ──
# Windows ไม่มี tz database ของระบบ — zoneinfo จึงต้องพึ่งแพ็กเกจนี้
# บน Linux/Docker ไม่จำเป็นแต่ใส่ไว้ให้ dev บน Windows รันได้เหมือนกัน
tzdata==2025.1

# ── ทดสอบ ──
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
pytest-cov==6.0.0
